import UploadHistory from '../components/UploadHistory';

export default function History() {
  return (
    <div>
      <h1>History</h1>
      <p>View recent uploads and reports.</p>
      <UploadHistory />
    </div>
  );
}
